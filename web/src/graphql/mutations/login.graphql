mutation Login($userLoginInput:UserLoginInput!){
    login(userLoginInput:$userLoginInput){
        errors {
            field
            message
        }

        user{
            email
            id
            password
            username
            firstname
            lastname
        }
    }
}